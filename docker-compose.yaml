version: '3.9'

services:
  mysql:
    image: mysql:latest
    environment:
      MYSQL_DATABASE: mydatabase        # fixed typo
      MYSQL_USER: myuser
      MYSQL_PASSWORD: secret
      MYSQL_ROOT_PASSWORD: verysecret
    ports:
      - "3307:3306"                    # map container port to host port